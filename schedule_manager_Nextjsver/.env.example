# --- 全般設定 ---
NODE_ENV=development
DEBUG=False

# --- ポート設定 ---
# Next.jsフロントエンドが使うポート
FRONTEND_PORT=3000
# Pythonバックエンドが使うポート
BACKEND_PORT=8000
# MySQLデータベースが使うポート
DB_PORT=3306

# --- データベース認証情報 ---
MYSQL_DATABASE=app_db
MYSQL_USER=user
MYSQL_PASSWORD=password
MYSQL_ROOT_PASSWORD=rootpassword

# --- バックエンド用データベースURL ---
# docker-compose.ymlの`backend`サービスで直接使われる
DATABASE_URL=mysql+pymysql://user:password@db/app_db

# --- CORS設定 ---
# フロントエンドのURLをカンマ区切りで指定（例: http://localhost:3000,http://example.com）
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://frontend:3000,http://host.docker.internal:3000

# --- フロントエンド用API設定 ---
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_INTERNAL_URL=http://backend:8000

# --- フロントエンドのDockerfile指定 ---
FRONTEND_DOCKERFILE=Dockerfile